import { makeStyles } from '@rneui/themed';

export default makeStyles(() => ({
  cCard: {
    justifyContent: 'center',
    height: 255,
    marginBottom: 2.5,
    padding: 0,
    elevation: 0,
    borderWidth: 0,
    shadowOpacity: 0,
  },
}));
