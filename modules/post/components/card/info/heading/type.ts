import { IsubTitle } from '../title/type';

export interface IbottomHeading {
  title: string;
  subTitle: IsubTitle;
}

export interface ItopHeading {
  people: number;
  isDecided: boolean;
  id: number;
}
