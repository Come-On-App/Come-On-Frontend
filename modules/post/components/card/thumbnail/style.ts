import { makeStyles } from '@rneui/themed';

export default makeStyles(() => ({
  ImageContianer: {
    height: 200,
    borderRadius: 8,
    marginBottom: 8,
  },
  TopContainer: {
    height: 36,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginVertical: 6,
    marginHorizontal: 12,
  },
}));
