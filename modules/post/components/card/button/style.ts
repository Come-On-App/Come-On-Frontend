import { makeStyles } from '@rneui/themed';

export default makeStyles(() => ({
  cButtonIcon: {
    width: 44,
    alignItems: 'center',
  },
  buttonIcon: {
    size: 24,
    color: '#231F20',
  },
}));
