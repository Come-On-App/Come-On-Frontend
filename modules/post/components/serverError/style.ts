import { makeStyles } from '@rneui/themed';

export default makeStyles(() => ({
  container: {
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  iconContainer: {
    height: '20%',
    width: '100%',
    alignSelf: 'center',
  },
}));
