import { makeStyles } from '@rneui/themed';

export default makeStyles(() => ({
  cMap: {
    height: 350,
    borderRadius: 8,
    overflow: 'hidden',
  },
  map: {
    height: '100%',
    width: '100%',
  },
}));
