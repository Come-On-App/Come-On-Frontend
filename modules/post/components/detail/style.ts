import { makeStyles } from '@rneui/themed';

export default makeStyles((theme) => ({
  dividerStyle: {
    width: 12,
    color: theme.font.grayscale['100'],
  },
  cContentHeader: { flexDirection: 'row', marginTop: 28, marginBottom: 12 },
}));
