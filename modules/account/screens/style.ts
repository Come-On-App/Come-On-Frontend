import { makeStyles } from '@rneui/themed';

export default makeStyles(() => ({
  userContainer: {
    alignItems: 'center',
  },
  msgContainer: {
    alignItems: 'center',
    marginTop: 4,
    marginBottom: 16,
  },
  cAccountManagement: {
    flex: 1,
    justifyContent: 'flex-end',
    alignItems: 'center',
  },
}));
