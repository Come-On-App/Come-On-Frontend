import { makeStyles } from '@rneui/themed';

export default makeStyles(() => ({
  userContainer: {
    alignItems: 'center',
  },
  msgContainer: {
    alignItems: 'center',
    marginTop: 4,
    marginBottom: 16,
  },
}));
