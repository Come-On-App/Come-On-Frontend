import { makeStyles } from '@rneui/themed';

export default makeStyles(() => {
  return {
    font: {
      color: '#4285F4',
    },
    button: {
      backgroundColor: '#FFFFFF',
      borderWidth: 1,
      borderColor: '#4285F4',
    },
  };
});
